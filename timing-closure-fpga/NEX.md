# Workspace notes

The active hardware project is the CPU FPGA prototype in `riscv-soc-prototype/`.
Its project guidelines live in `riscv-soc-prototype/NEX.md` — read that file first and
follow it for any work on the prototype (paths in it are relative to `riscv-soc-prototype/`).
